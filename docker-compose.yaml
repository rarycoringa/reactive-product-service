services:
  product:
    build: .
    container_name: product
    environment:
      - SERVER_PORT=8282
    ports:
      - "8282:8282"
    networks:
      - reactive-ms-network

networks:
  reactive-ms-network:
    external: true